# Bypass CRA's strict peer-dependency pre-flight check.
# Required because react-scripts 5.x was built for React 16-18,
# but this project uses React 19 (fully compatible at runtime).
SKIP_PREFLIGHT_CHECK=true
